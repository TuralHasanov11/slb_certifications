App As appinfo:
    BackEnabled: =false
    OnStart: |
        =Concurrent(
            Set(CurrentUserEmail, User().Email),
            Set(currentPage, {page: "UsersPage"}),
            Set(PrimaryColor, "#003366")
        )
    StartScreen: =

